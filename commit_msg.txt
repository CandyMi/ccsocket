fix(ccsocket): collect all DNS answer IPs + TTL, fix segfault

- ccaddrinfo_t now includes uint32_t ttl field
- on_dns_answer copies ttl and properly appends all A/AAAA records
- Fix tail pointer NULL dereference in on_dns_answer (segfault)
- IP detection uses ccsocket_get_version() instead of raw inet_pton
- CMake: add CMP0063 policy to suppress visibility warnings
